% Typography
\usepackage{microtype}
\usepackage{csquotes}

% Colors
\usepackage{xcolor}
\definecolor{careitblue}{HTML}{1F4E79}

% TikZ (for title page graphics)
\usepackage{tikz}
\usetikzlibrary{calc}

% Header / footer (KOMA-Script)
\usepackage{scrlayer-scrpage}
\clearpairofpagestyles
\automark[section]{chapter} % <-- important: defines \headmark behavior
\ihead{CARE-IT}
\ohead{\headmark}
\cfoot{\pagemark}

% Calm spacing (KOMA-native)
\RedeclareSectionCommand[
  beforeskip=1.0\baselineskip,
  afterskip=0.6\baselineskip
]{chapter}

\RedeclareSectionCommand[
  beforeskip=0.8\baselineskip,
  afterskip=0.4\baselineskip
]{section}

% Paragraph headings (Pandoc ####) for term entries
% Make them NOT run-in and give breathing space
\setkomafont{paragraph}{\normalfont\normalsize\bfseries}
\RedeclareSectionCommand[
  runin=false,
  beforeskip=1.2em,
  afterskip=0.4em
]{paragraph}

% Subtle readability improvement
\linespread{1.05}

% Hyperref styling (Pandoc loads hyperref; apply settings after load)
\AtBeginDocument{%
  \hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=careitblue,
    citecolor=black
  }%
}